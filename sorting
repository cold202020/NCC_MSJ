n=int(input())
c=[]
d=[]
for i in range(n):
    a,b=(input().split())
    x=int(a)
    y=int(b)
    c.append(x)
    d.append(y)
for i in range(len(c)):
    if c[i]<d[i]:
        print(c[i]," ",d[i])
    else:
        print(d[i]," ",c[i])
